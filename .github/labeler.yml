# Add 'Frontend' label to any change within the 'frontend' folder
Frontend:
  - "frontend/**"

# Add 'backend' label to any change within the 'server' folder
backend:
  - "server/**"

# Add 'documentation' label to any markdown files
documentation:
  - "**/*.md"
